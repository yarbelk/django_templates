# Custom app settings to further polute  your DJANGO_SETTINGS_MODULE
# with setting creep
# Usage:
# For any model, view etc that uses a setting, import from here, and it will
# be either the default or what was placed in the DJANGO_SETTINGS_MODULE.

#from django.conf import settings
#SETTINGS_NAME = getattr(settings,'SETTINGS_NAME',<default_value>)
